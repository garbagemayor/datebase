#include "SQLParser.h"

using namespace std;

bool SQLParser::parse(const std::string& sql, SQLParserResult* result)
{
	return true;
}

bool SQLParser::tokenize(const std::string& sql, std::vector<int>* result)
{
	return true;
}
